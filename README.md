# Lookback Monorepo Scaffold

## Structure

- `backend/`: FastAPI service with API routers, service layer, and data models.
- `frontend/`: Next.js App Router app with timeline and review pages.
- `shared/`: JSON Schemas for cross-service event contracts.
- `infra/`: Local infrastructure and deployment notes.
